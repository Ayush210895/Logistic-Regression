[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "logistic-regression-from-scratch"
version = "0.1.0"
description = "Binary logistic regression implemented from scratch with gradient descent."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
